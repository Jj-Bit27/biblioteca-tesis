-- ACTUALIZEN LAS NOTAS!!!

-- Lo que se modifico fue casi todo, ahora no se hace el fetch directo si no mediante axios mas rapido y mejor

-- Los listados, eran innecesarios crear un "NO FETCH", solo era modificarlos cosa que no se hicieron y eran
    muchos archivos innecesarios

-- La cookies fue innecesario, al final se hizo lo mismo solo que una autenticacion que viene desde el backend
    y esta vez si funciona

-- Al crear un nuevo dato, ahora se actualiza  se muestra hasta arriba como el primero y recien creado

-- Entre otras cosas, que funcionan mejor y ya.

-- Cuando instalen nuevas bibliotecas, se instalan en el carpeta correspondiente, al menos que se use global,
    se puede instalar en la carpeta principal